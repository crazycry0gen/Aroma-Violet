@model IEnumerable<Aroma_Violet.Models.EmailTemplate>

@{
   
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmailTemplateName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SqlStatement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailAddress)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmailBody)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmailTemplateName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SqlStatement)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmailBody)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.EmailTemplateId }) |
                @Html.ActionLink("Details", "Details", new { id=item.EmailTemplateId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.EmailTemplateId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
