@model IEnumerable<Aroma_Violet.Models.AccountHolder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AccountHolderName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AccountHolderName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccountHolderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccountHolderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccountHolderId })
        </td>
    </tr>
}

</table>
