@model IEnumerable<Aroma_Violet.Models.PostageCharge>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostageChargeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Charge)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostageChargeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Charge)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostageChargeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostageChargeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostageChargeId })
        </td>
    </tr>
}

</table>
