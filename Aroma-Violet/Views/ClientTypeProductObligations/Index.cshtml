@model IEnumerable<Aroma_Violet.Models.ClientTypeProductObligation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th colspan="3">
            @Html.DisplayNameFor(model => model.ClientType.ClientTypeName)
        </th>
        <th colspan="5">
            @Html.DisplayNameFor(model => model.Product.ProductName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th colspan="2"></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td colspan="3">
            @Html.DisplayFor(modelItem => item.ClientType.ClientTypeName)
        </td>
        <td colspan="5">
            @Html.DisplayFor(modelItem => item.Product.ProductName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td colspan="2">
            @Html.ActionLink("Edit", "Edit", new { id=item.ClientTypeProductObligationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ClientTypeProductObligationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ClientTypeProductObligationId })
        </td>
    </tr>
}

</table>
