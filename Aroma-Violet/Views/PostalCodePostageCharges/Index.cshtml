@model IEnumerable<Aroma_Violet.Models.PostalCodePostageCharge>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostageCharge.PostageChargeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostalCode.PostalCodeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostageCharge.PostageChargeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostalCode.PostalCodeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostalCodePostageChargeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostalCodePostageChargeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostalCodePostageChargeId })
        </td>
    </tr>
}

</table>
