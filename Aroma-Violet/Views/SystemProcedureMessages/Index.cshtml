@model IEnumerable<Aroma_Violet.Models.SystemProcedureMessage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SystemProcedure.ProcedureName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MessageDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Message)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SystemProcedure.ProcedureName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MessageDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Message)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SystemProcedureMessageId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SystemProcedureMessageId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SystemProcedureMessageId })
        </td>
    </tr>
}

</table>
