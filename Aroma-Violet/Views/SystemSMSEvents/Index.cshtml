@model IEnumerable<Aroma_Violet.Models.SystemSMSEvent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SystemSMSTemplate.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SystemSMSEventName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Active)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SystemSMSTemplate.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SystemSMSEventName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Active)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SystemSMSEventId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SystemSMSEventId }) |
            
            @Html.ActionLink("Edit Template", "Edit", "SystemSMSTemplates", new { id = item.SystemSMSTemplateId }, null)
        </td>
    </tr>
}

</table>
